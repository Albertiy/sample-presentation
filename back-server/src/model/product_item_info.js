class ProductItemInfo {
    id;
    itemId;

    /**
     * 
     * @param {number} id 
     * @param {number} itemId 
     */
    constructor(id, itemId) {
        this.id = id;
        this.itemId = itemId;
    }
}

module.exports = ProductItemInfo